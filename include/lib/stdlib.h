// SPDX-License-Identifier: MIT
// Copyright (c) 2025 Chloe Eather

#ifndef CEEK_LIBC_STDLIB_H_
#define CEEK_LIBC_STDLIB_H_

#include <lib/itoa.h>
#include <lib/atoi.h>

#define max(x, y) ((x) > (y) ? (x) : (y))
#define min(x, y) ((x) < (y) ? (x) : (y))

#define NULL (0ull)

#endif

